# MysticStars Environment Configuration
# Copy this file to .env and fill in the values

# =============================================================================
# Database (PostgreSQL)
# =============================================================================

POSTGRES_USER=mysticstars
POSTGRES_PASSWORD=mysticstars_pass_change_me
POSTGRES_DB=mysticstars

# =============================================================================
# Backend API
# =============================================================================

NODE_ENV=production

# Database connection string
# Docker: Use postgres as hostname
POSTGRES_URI=postgresql://mysticstars:mysticstars_pass_change_me@postgres:5432/mysticstars

# AI Service API (required for generating readings)
BLACKBOX_API_KEY=your-blackbox-api-key-here

# Optional: Force a specific model (avoids reasoning models that can fail)
# Recommended: blackboxai/deepseek/deepseek-chat or blackboxai/meta-llama/llama-4-scout
BLACKBOX_MODEL=blackboxai/deepseek/deepseek-chat

# Internal API URL (used by cronjob service)
API_BASE_URL=http://api:3000

# Frontend URL for CORS (your public domain)
FRONTEND_URL=https://mysticstars.app

# =============================================================================
# Optional: Telegram Alerts
# =============================================================================

# TELEGRAM_BOT_TOKEN=your-telegram-bot-token
# TELEGRAM_CHAT_ID=your-telegram-chat-id

# =============================================================================
# Scheduler Service (Bull Board UI)
# =============================================================================

# Redis connection (for BullMQ job queue)
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password-if-needed

# Scheduler admin UI authentication (recommended for production)
SCHEDULER_ADMIN_USER=admin
SCHEDULER_ADMIN_PASSWORD=change-this-password
